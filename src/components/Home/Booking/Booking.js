import React from 'react';
import { useParams } from 'react-router';

const Booking = () => {
    const {_id} =useParams()
    return ( 
        <div>
            
        </div>
    );
};

export default Booking;